import { AsteriskDirective } from './asterisk.directive';

describe('AsteriskDirective', () => {
  it('should create an instance', () => {
    const directive = new AsteriskDirective();
    expect(directive).toBeTruthy();
  });
});
